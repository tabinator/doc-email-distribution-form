# DOC Status Notification Form

Static frontend prototype for DOC activation, deactivation, and status update
notification requests.

Open `index.html` in a browser to preview locally. The submit button currently
uses a mock response until `REST_ENDPOINT` is configured in `script.js`.
