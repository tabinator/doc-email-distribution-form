# DOC Status Notification Form

Static frontend prototype for DOC activation, deactivation, and status update
notification requests.

Open `index.html` in a browser to preview locally. The submit button posts the
reviewed payload to `REST_ENDPOINT` in `script.js`, then downloads the PDF
returned by the service.
